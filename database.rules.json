{
  "rules": {
    ".read": false,
    ".write": false,
    "sync": {
      "$uid": {
        ".read": "auth != null && $uid === auth.uid",
        ".write": "auth != null && $uid === auth.uid"
      }
    },
    "usernames": {
      ".read": false,
      "$username": {
        ".read": true,
        ".write": "auth != null && auth.token.email != null && newData.isString() && newData.val() === auth.token.email && (!data.exists() || data.val() === auth.token.email)",
        ".validate": "auth != null && auth.token.email != null && newData.isString() && newData.val() === auth.token.email"
      }
    }
  }
}
