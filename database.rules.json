{
  "rules": {
    ".read": false,
    ".write": false,
    "sync": {
      "$uid": {
        ".read": "auth != null && $uid === auth.uid",
        ".write": "auth != null && $uid === auth.uid"
      }
    },
    "usernames": {
      ".read": false,
      "$username": {
        ".read": true,
        ".write": "auth != null && auth.token.email != null && $username.matches(/^[a-z0-9_]{3,16}$/) && (!data.exists() || data.val() === auth.token.email) && newData.val() === auth.token.email",
        ".validate": "newData.isString() && newData.val() === auth.token.email"
      }
    }
  }
}
